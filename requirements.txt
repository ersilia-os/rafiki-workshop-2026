# The app's runtime, and the only file Streamlit Community Cloud installs -
# with pip. The data-preparation scripts have their own, in scripts/.
#
# packages.txt is required after all, and supplies libxrender1 and libxext6.
#
# The reasoning for dropping it was that Community Cloud runs a full Debian
# trixie, which provides libXrender and libXext itself. That held for the image
# behind Python 3.11. Cloud now serves this app on Python 3.14, whose image does
# not carry them, and the app died on import with exactly the
# "ImportError: libXrender.so.1" that was named as the signal the assumption had
# broken.
#
# The risk that came with putting it back: the image has carried a leftover
# bullseye-security apt source, and bullseye LTS ended 2026-08-31, so its
# Release file is expired. If apt-get exits non-zero the build aborts before pip
# runs - the build log will say so plainly, and the fallback is conda.
#
# Versions resolved on linux-64.
streamlit==1.63.0
pandas==3.0.5
numpy==2.4.6
rdkit==2026.3.6
altair==6.2.2
scikit-learn==1.9.0
